<nav class="hh-sheet-tabs tabs" data-group="primary" data-application-part="tabs">
  {{#each tabs as |tab|}}
  <a data-action="tab" class="{{tab.cssClass}}" data-tab="{{tab.id}}" data-group="{{tab.group}}"
    title="{{localize tab.label}}">{{localize tab.label}}</a>
  {{/each}}
</nav>
