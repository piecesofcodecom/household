{{!-- Shared "you must give up a Success" notice. Used by both the skill-roll
      card (skill-roll-card.hbs) and the plain /roll card (dice-roll.hbs) so the
      two stay visually identical. --}}
<span class="hh-roll-notice"><i class="fa fa-triangle-exclamation"></i> {{localize "MESSAGES.Gaveup"}}</span>
